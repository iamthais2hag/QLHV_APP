# IM_GPLX local student photos

This folder is for local student photo files only.

Do not commit real student photos.

Expected structure:

```text
IM_GPLX/{MaKhoa}/{MaDK}.jp2
```

The backend resolves photos from `MaKhoa` and `MaDK`; it must not expose physical disk paths to the frontend.
