# V1/BCII owner approval checklist

Phạm vi duyệt: V2 → V1. Không mục nào được đánh dấu sẵn.

## A. TUYỆT ĐỐI KHÔNG GHI

| Domain | Column/table | Proposed rule | Evidence | Risk if wrong | Approval |
| --- | --- | --- | --- | --- | --- |
| V1 whole tables | `BaoCaoII`; `KySH`; `DM_LyDoTCBC2`; `DM_DiemSatHach`; `DM_KetQuaSatHach` | Không INSERT/UPDATE/DELETE/RECONCILE từ V2. | Ngoài catalog; lookup policy từ chối; không có auto-discovery. | Phá hủy dữ liệu BCII/sát hạch. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX_HoSo | `NoiDungSH`; `MaBC2`; `KetQuaBC2`; `MaLyDoTCBC2`; `MaKySH`; `SoBD`; `LanSH`; `SoQDSH`; `NgayQDSH`; `KetQua_LyThuyet`; `NhanXet_LyThuyet`; `KetQuaSHM`; `NhanXet_MoPhong`; `KetQua_Hinh`; `NhanXet_Hinh`; `KetQua_Duong`; `NhanXet_Duong`; `KetQuaSH`; `SoQDTT`; `NgayQDTT`; `NguoiKy`; `SoGPLXTmp`; `NgayKTBC2`; `NguoiKTBC2`; `MaIn`; `KetQuaDoiSanhTW`; `GhiChuKQDSTW`; `ChuKy`; `TT_XuLy_Old`; `CHON_IN_GPLX`; `KetQuaPDSo`; `DAT_QDThucHanh`; `DAT_TGThucHanh`; `DAT_KQCuc`; `DAT_ThoiGianLayKQ`; `LyDoTuChoiKQDT` | 36 cột: không INSERT/UPDATE từ V2; không clear/delete. | Policy V1-owned loại khỏi projection; writer chung tuân policy. | Ghi đè dữ liệu BCII/sát hạch. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX_GPLX | Whole table, all 53 columns | Automatic forward write disabled; không INSERT/UPDATE/DELETE. | Planner trả `Include=false` cho domain. | Lộ/sai dữ liệu GPLX và danh tính học viên. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| All domains | 29 target-default/audit columns | Không đọc/ghi từ V2; V1 sinh giá trị; immutable khi UPDATE. | Acceptance `TARGET_DEFAULT`/`IMMUTABLE`; policy loại khỏi source projection. | Hỏng audit và định danh nội bộ. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX_HoSo | `CoQuanQuanLyGPLX`; `MaHTCap`; `QDThucHanhHinh`; `Transfer_flag` | Không ghi vì chưa có nghĩa đích V1 được duyệt. | Acceptance `SKIP_V2_ONLY`; policy loại khỏi projection. | Ghi nhầm cột hoặc sai nghĩa nghiệp vụ. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| All business domains | Every row, FK and BCII relation | `NO_DELETE`; `NO_CLEAR_RELATION`; `PRESERVE_V1_TARGET`. | Không có target DELETE; tombstone chỉ ở state/diagnostics. | Orphan hoặc mất hồ sơ/BCI/BCII. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |

## B. CHỈ GHI TRƯỚC LIFECYCLE V1

| Domain | Column/table | Proposed rule | Evidence | Risk if wrong | Approval |
| --- | --- | --- | --- | --- | --- |
| NguoiLX_HoSo | `TT_XuLy` | Row mới chỉ nhận `03/04/09`; V2 chỉ ghi trước strong lifecycle; sau đó preserve V1. | Strong lock từ BCII/sát hạch hoặc trạng thái `11,12,13,14,16,17,18,19`; bỏ qua sentinel/default. | Ghi đè hoặc khóa nhầm dữ liệu V1. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX_HoSo | `TrangThai` | V2 chỉ ghi trước strong lifecycle; sau đó preserve V1. | Strong predicate đã đề xuất; default `TrangThai` không phải evidence. | Ghi đè hoặc khóa nhầm dữ liệu V1. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX_HoSo | `MaKhoaHoc` | V2 chỉ ghi trước strong lifecycle/relation lock; sau đó preserve V1. | Dossier lifecycle và quan hệ BCI/BCII. | Làm lệch khóa học tham gia BCII. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX_HoSo | `MaBC1` | V2 chỉ ghi trước strong lifecycle/relation lock; sau đó preserve V1. | Dossier lifecycle hoặc `BaoCaoII.MaBCI` liên quan. | Làm lệch hoặc orphan BCII. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX_HoSo | `GhiChu` | V2 chỉ ghi trước strong lifecycle; sau đó preserve V1. | Strong predicate đã đề xuất; blank/default bị bỏ qua. | Ghi đè ghi chú nghiệp vụ V1. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX_HoSo | `GiaiTrinh` | V2 chỉ ghi trước strong lifecycle; sau đó preserve V1. | Strong predicate đã đề xuất; blank/default bị bỏ qua. | Ghi đè giải trình V1. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX_HoSo | `GiayCNSK` | V2 chỉ ghi trước strong lifecycle; sau đó preserve V1. | Strong predicate đã đề xuất; blank/default bị bỏ qua. | Ghi đè dữ liệu sức khỏe do V1 quản lý. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |

## C. V2 ĐƯỢC QUYỀN GHI

| Domain | Column/table | Proposed rule | Evidence | Risk if wrong | Approval |
| --- | --- | --- | --- | --- | --- |
| DM_DonViGTVT | `CacHangGPLX`; `CoQuanQL`; `DiaChi`; `DiaDiemDaoTao`; `DienThoai`; `DienTichSanTap`; `Fax`; `GhiChu`; `LanhDao`; `LoaiDV`; `LoaiTTSH`; `LuuLuongDT`; `MaDvOld`; `MaDVQL`; `NgayGP`; `NgayHHGP`; `SoGP`; `TenDV`; `TrangThai`; `Website` | Cho INSERT/UPDATE sau validation/transform đã duyệt. | 20 cột unconditional V2-owned. | Sai authority hoặc truncation. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| GiaoVien | `AnhCD`; `CacHangGPLXDuocDT`; `CauTaoSuaChua`; `DaoDucLaixe`; `DienThoai`; `GhiChu`; `GioiTinh`; `HangGPLX`; `HinhThuc_TuyenDung`; `HoTenDem`; `KyThuatLaixe`; `LoaiHinh_DaoTao`; `LuatGTDB`; `MaCSDT`; `MaFileTiepNhanXML`; `MaSoGTVT`; `NgayCapGPLX`; `NgayQD_GCN`; `NgaySinh`; `NghiepVuVanTai`; `NoiCT`; `NoiCT_MaDVHC`; `NoiCT_MaDVQL`; `SoCMT`; `SoQD_GCN`; `TenGV`; `ThamNien_LaiXe`; `ThoiGianTiepNhanXML`; `TrangThai`; `TrinhDo_ChuyenMon`; `TrinhDo_SuPham`; `TrinhDo_VanHoa` | Cho INSERT/UPDATE sau validation/transform đã duyệt; domain vẫn optional/fail-closed. | 32 cột unconditional V2-owned. | Sai enum/mã hoặc truncation. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| KhoaHoc | `GhiChu`; `HangDT`; `HangGPLX`; `HTDaoTao`; `MaCSDT`; `MaSoGTVT`; `MucTieuDT`; `NgayBG`; `NgayKG`; `NgayQD_KhaiGiang`; `NgaySH`; `NgayThi`; `SoHVDuocCapGPLX`; `SoHVTotNghiep`; `SoNgayNghiLe`; `SoNgayOnKT`; `SoNgayThucHoc`; `SoQD_KhaiGiang`; `TenKH`; `ThoiGianDT`; `TongSoHV`; `TongSoNgay`; `TrangThai`; `TT_Xuly` | Cho INSERT/UPDATE trước relation lock; `MaKH` vẫn insert-only. | 24 cột unconditional V2-owned. | Làm lệch khóa học và quan hệ BCI/BCII. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| KhoaHoc_GiaoVien | `BienSoXe`; `GhiChu`; `IsKhoaHocGiaoVien`; `LoaiGV`; `MaGV`; `MaKH`; `MaMonHoc`; `NgayBD`; `NgayHetHL`; `NgayHL`; `NgayKT`; `SoHV`; `TenGV`; `TenMonHoc`; `TrangThai` | Cho INSERT/UPDATE theo validation; composite key vẫn insert-only. | 15 cột unconditional V2-owned. | Sai liên kết khóa học–giáo viên. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| BaoCaoI | `BoTriHocVienXeTap`; `GhiChu`; `LuuLuong`; `LuuLuongGP`; `MaCSDT`; `MaKH`; `NgayBaoCao`; `NgayBG`; `NgayCapGP`; `NgayKG`; `NgayTiepNhan`; `NguoiTiepNhan`; `SoBaoCao`; `SoGP`; `SoHocSinh`; `SoHSCanhBao`; `ThoiGianDaoTao`; `ThoiGianTiepNhan`; `TrangThai`; `TT_Xuly` | Cho INSERT/UPDATE trước BCII/relation lock; `MaBCI` vẫn insert-only. | 20 cột unconditional V2-owned. | Làm lệch BCI đã tham gia BCII. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX | `DonViNhanHSo`; `GhiChu`; `GioiTinh`; `HoDemNLX`; `HoVaTen`; `HoVaTenIn`; `MaQuocTich`; `NgayCapCMT`; `NgaySinh`; `NoiCapCMT`; `NoiCT`; `NoiCT_MaDVHC`; `NoiCT_MaDVQL`; `NoiTT`; `NoiTT_MaDVHC`; `NoiTT_MaDVQL`; `SO_CMND_CU`; `SoCMT`; `TenNLX`; `TrangThai` | Cho INSERT/UPDATE sau validation; `MaDK` vẫn insert-only; không DELETE. | 20 cột unconditional V2-owned. | Sai dữ liệu định danh học viên. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX_HoSo | `BC1_ThamNien`; `BC1_TuoiTS`; `ChatLuongAnh`; `DiemKQLyThuyet`; `DiemKQThucHanh`; `DonViCapGPLXDaCo`; `DonViHocLX`; `DuongDanAnh`; `HangDaoTao`; `HangGPLX`; `HangGPLXDaCo`; `KetLuanCSDT`; `KQ_BC1`; `KQ_BC1_GhiChu`; `KQLyThuyet`; `KQThucHanh`; `LyDoCapDoi`; `MaCSDT`; `MaDVNhanHSo`; `MaLoaiHs`; `MaSoGTVT`; `MaTrichNgang`; `MucDichCapDoi`; `NamcapLandau`; `NamHocLX`; `NgayCapCCN`; `NgayCapGPLXDaCo`; `NgayHenTra`; `NgayHHGPLXDaCo`; `NgayInGiayTN`; `NgayKTBC1`; `NgayNhanHSo`; `NgayRaQDTN`; `NgayThuNhanAnh`; `NgayTTGPLXDaCo`; `NgayVaoSoCNN`; `NgayVaoSoTN`; `NguoiKTBC1`; `NguoiNhanHSo`; `NguoiThuNhanAnh`; `NoiCapGPLXDaCo`; `SoCCN`; `SoGiayCNTN`; `SoGPLXDaCo`; `SoHoSo`; `SoKmLXAnToan`; `SoNamLX`; `SoQuyetDinhTN`; `SoSoTN`; `TGBatDau`; `TGKetThuc`; `TGThucHanhDuong`; `TGThucHanhHinh`; `TongQDThucHanh`; `VaoSoCNNSo`; `XepLoaiTotNghiep` | Cho INSERT/UPDATE sau validation; không làm suy yếu 36 V1-owned và 7 conditional fields. | 56 cột unconditional V2-owned. | Có thể làm lệch hồ sơ đào tạo. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLXHS_GiayTo | `SoHoSo`; `TenGT`; `TrangThai` | Cho INSERT/UPDATE khi parent được chấp nhận; key `(MaGT,MaDK)` insert-only; không DELETE. | 3 cột unconditional V2-owned. | Tạo giấy tờ orphan hoặc sai liên kết. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |

## D. CHƯA XÁC ĐỊNH — PHẢI CHỦ DỰ ÁN QUYẾT ĐỊNH

| Domain | Column/table | Proposed rule | Evidence | Risk if wrong | Approval |
| --- | --- | --- | --- | --- | --- |
| XeTap; KhoaHoc_XeTap; DM_LuuLuongDaoTao | 59 columns marked `UNCLASSIFIED` in the acceptance matrix | Giữ fail-closed; chưa thêm catalog/policy cho tới khi duyệt owner và mapping từng cột. | Hiện cả ba domain ngoài catalog; 34 + 17 + 8 cột chưa phân loại. | Có thể ghi V1-owned/audit hoặc tạo quan hệ sai. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| GiaoVien | `CacHangDaCo`; `CacMonHoc`; `LoaiGiaoVien`; `NgayHHGPLX`; `NoiCapGCN` | Quyết định bỏ, thêm cột đích hay mapping; hiện giữ `BLOCK_DOMAIN`. | Matrix cấm nhưng policy hiện cấp quyền V2; writer chỉ an toàn nhờ schema gate. | Schema drift có thể biến thành ghi ngoài ý muốn. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| GiaoVien | `HangGPLX`; `HinhThuc_TuyenDung`; `LoaiHinh_DaoTao`; `GhiChu` | Duyệt mapping/validation lossless trước khi cho ghi. | Acceptance yêu cầu transform/validation; code chưa thực hiện transform. | Mất nghĩa enum/mã hoặc truncation. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| DM_DonViGTVT | `CoQuanQL`; `TenDV` | Duyệt fixed-authority và lossless-fit rule. | Acceptance yêu cầu transform/validation; exact-type gate hiện chặn trường hợp không tương thích. | Sai cơ quan quản lý hoặc truncation. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX_HoSo | Lifecycle signals ngoài strong predicate đề xuất | Chỉ bổ sung sau khi xác nhận sentinel/default và chuẩn hóa blank/whitespace. | Detector hiện dùng non-null cho nhiều cột có default và bỏ sót `MaLyDoTCBC2`. | False positive khóa V2; false negative ghi đè V1. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| NguoiLX_GPLX | Whole-domain operating contract | Xác nhận full V1 ownership, automatic write disabled, và domain phải non-blocking. | Writer không ghi nhưng catalog hiện đánh dấu mandatory. | Một domain preserve-only có thể dừng toàn luồng. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
| Runtime diagnostics | Raw matching key versus diagnostic identity | Raw key chỉ tồn tại transient cho exact matching; persisted/API/UI/log dùng SHA-256 identity hash only. | Tombstone/state/API/frontend hiện có raw `SourceKey`/`MaDK`. | Lộ PII và tạo hợp đồng API không an toàn. | [ ] APPROVE<br>[ ] REJECT<br>[ ] MODIFY |
